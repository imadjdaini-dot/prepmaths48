/**
 * Constantes partagées serveur/client — sans dépendance (importable depuis
 * un composant client sans alourdir le bundle).
 */

/** Nombre maximal d'élèves par import groupé (API + formulaire admin). */
export const BULK_STUDENTS_MAX = 100;
